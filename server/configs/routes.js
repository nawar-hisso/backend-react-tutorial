// Constants for route names
const HOME = {
  ROOT: '/',
};

const BLOGS = {
  ROOT: '/blogs',
  LIST: '/list',
  GET: '/:id',
  CREATE: '/create',
  DELETE: '/:id',
};

const ROUTES = {
  HOME,
  BLOGS,
};

export default ROUTES;
